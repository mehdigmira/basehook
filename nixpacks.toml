[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -e ."
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "uvicorn basehook.api:app --host 0.0.0.0 --port $PORT"
